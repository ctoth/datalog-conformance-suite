source: souffle/evaluation/singleton
tags:
- evaluation
- souffle
tests:
- name: souffle_singleton_query
  description: Harvested from Souffle testcase singleton, asserting exported relation
    query.
  tags:
  - basic
  - evaluation
  - souffle
  program:
    facts: {}
    rules:
    - query("t2") :- a(_x).
    - query("t3") :- a(_).
    - query("t4") :- b(x).
    - query("t5") :- b(_x).
    - query("t6") :- b(_).
    - query("t7") :- a(x), b(y).
    - query("t8") :- a(x), a(y).
    - query("t9") :- b(x), b(y).
  expect:
    query:
    - - t1
    - - t2
    - - t3
    - - t8
